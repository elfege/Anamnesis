# Portfolio-scrub allowlist for Anamnesis public mirror.
# One rule per line. Rules are evaluated top→bottom; first match wins.
# Format: <action> <glob>
#   ALLOW  <glob>   — file goes to public mirror verbatim
#   STRIP  <glob>   — file is REMOVED from the public mirror push (kept in private-dev)
#
# Per MSG-299: per-file decisions, NOT blanket *.md strip. Per MSG-297..302
# leak-defense pattern from TILES/NVR adoption. Per RULE 19.1.3 plans are
# intended-untracked so they should not be in git at all — but this file
# also protects against accidental re-tracking.

# === Explicit strips (internal artifacts) ===
STRIP  docs/plans/**
STRIP  docs/bitter_lesson/**
STRIP  docs/research/**
STRIP  docs/README_handoff.md
STRIP  docs/README_project_history.md
STRIP  README_handoff.md
STRIP  README_project_history.md
STRIP  CLAUDE.md
STRIP  CLAUDE.md.bak.*
STRIP  chat_anamnesis.md
STRIP  .claude/**
STRIP  .runpod_pod_id.*
STRIP  voices_data/**
STRIP  samples/**

# === Fallback: default-ALLOW ===
# Anamnesis has a rich, evolving code tree (app/, avatar_worker/, d2/,
# trainers/, scripts/, tests/, etc.). Enumerating every allowed path
# turns every new subdir into a mirror-push failure — high false-positive
# rate. Instead we default-ALLOW and STRIP only the known-sensitive
# categories above. New sensitive artifacts must be added to the STRIP
# list explicitly when introduced.
ALLOW  **
