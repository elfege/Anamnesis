# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
.venv/
venv/

# Docker volumes (local data)
mongo_data/
model_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
.claude

# Private operational files — never track
CLAUDE.md
README_*.md
# …except documented rules (verbose-name pattern flags them as canonical)
!README_isolation_rule_*.md
!README_canonical_*.md
0_ANAMNESIS_SOURCES/
*.log



# Docs — only HTML published, not markdown
docs/*.md
0_ANAMNESIS_DOCS_http.server.sh



# Avatar/persona samples — images + audio sources (may be copyright, large)
samples/

# ─── Docker security policy (see Dockerfile.example for rules) ────────────────
# Actual Dockerfiles are gitignored — only `.example` templates are tracked.
# This forces every clone to consciously copy the template, with the security
# header front-and-center, before building. See scripts/security_check.sh too.
Dockerfile
Dockerfile.*
!Dockerfile*.example
!*.dockerignore   # .dockerignore files MUST stay tracked — they're the seatbelt

# Trained model weights (personal-data-encoded — never publish via the runtime image)
d2/d2_checkpoints/
d2/output/
d2_checkpoints/
checkpoints/
*.pt
*.bin
*.safetensors
*.ckpt
*.gguf

# Pre-push security check script (intentionally untracked — easy to edit per machine)
scripts/security_check.sh
app/uploads/

# Bind-mounted container state (per canonical rule 2026-06-11) — survives
# rebuild/restart, captured by ~/-based backup, NEVER tracked in git.
.miopen_cache/
.hf_cache/
.torch_cache/
.mongo_data/
.mongo_configdb/
.anamnesis_voices/
.model_cache/

# RunPod pod state files (ephemeral per pod cycle)
.runpod_pod_id
.runpod_pod_id.*

# Voice sample storage (bind mount, not in repo)
voices_data/
